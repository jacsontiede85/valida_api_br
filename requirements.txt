# Core dependencies
playwright>=1.40.0
crawl4ai>=0.3.0
pydantic>=2.0.0
structlog>=23.1.0
python-dotenv>=1.0.0
aiofiles>=23.1.0
pytz>=2023.3  # Para timezone brasileiro nos logs

# HTTP/API
aiohttp>=3.8.0
httpx>=0.24.0
requests>=2.31.0

# FastAPI dependencies for API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic-settings>=2.0.0
python-multipart>=0.0.6

# MariaDB/MySQL dependencies (migração de Supabase para MariaDB local)
PyMySQL>=1.1.0
cryptography>=41.0.0  # Para suporte SSL no PyMySQL

# Authentication & Security
PyJWT>=2.8.0  # Para JWT tokens
python-jose[cryptography]>=3.3.0  # JWT com crypto support
passlib[bcrypt]>=1.7.4  # Password hashing com bcrypt
bcrypt>=4.0.1  # Para hashing seguro de senhas

# Email handling
imaplib2>=0.57
email-validator>=2.0.0

# Payment Processing & SaaS
stripe==6.5.0  # Integração com Stripe (v6.5.0 - versão estável sem bug do stripe.apps)
python-decimal>=1.0.0  # Para cálculos precisos de valores monetários

# Date & Time
python-dateutil>=2.8.2  # Manipulação avançada de datas

# Data processing
pandas>=2.0.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Monitoring & Observability
prometheus-client>=0.19.0  # Métricas Prometheus
sentry-sdk[fastapi]>=1.40.0  # Error tracking e monitoring

# Background Tasks & Scheduling
celery>=5.3.0  # Para tasks assíncronas
redis>=5.0.0  # Backend para Celery

# File Handling & Utilities
openpyxl>=3.1.0  # Para manipulação de Excel
python-magic>=0.4.27  # Detecção de tipos de arquivo

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Development
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0
